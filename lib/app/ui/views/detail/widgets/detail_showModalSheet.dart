import 'package:flutter/material.dart';

class DetailShowModalSheet extends StatelessWidget {
const DetailShowModalSheet({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}